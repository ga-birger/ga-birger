export interface NavLink {
  href: string;
  label: string;
}

export interface SocialLink {
  href: string;
  label: string;
  icon: string;
}

export interface PageMeta {
  title: string;
  description: string;
  keywords?: string[];
}

